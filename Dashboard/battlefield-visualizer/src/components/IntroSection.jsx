// src/components/HeroSection.jsx
const HeroSection = () => (
  <section className="hero">
    <h1>Real-Time Battlefield Intelligence Dashboard</h1>
    <p>
      In combat zones, life-saving decisions are delayed due to fragmented or slow data.
      This platform brings together casualty locations, threat zones, and supply routes
      on a unified interface for faster, smarter decisions.
    </p>
  </section>
);

export default HeroSection;
